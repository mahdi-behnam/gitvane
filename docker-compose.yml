services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: gitvane-postgres
    environment:
      POSTGRES_USER: gitvane
      POSTGRES_PASSWORD: gitvane
      POSTGRES_DB: gitvane
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gitvane"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: gitvane-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gitvane-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://gitvane:gitvane@postgres:5432/gitvane
      - SYNC_DATABASE_URL=postgresql+psycopg://gitvane:gitvane@postgres:5432/gitvane
      - REDIS_URL=redis://redis:6379/0
      - GITVANE_WORKSPACE=/workspace/repos
      - APP_NAME=GitVane
      - ENVIRONMENT=docker
      - DEBUG=true
    volumes:
      - ./backend:/app
      - repos_volume:/workspace/repos
      - huggingface_cache:/root/.cache/huggingface
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000/api/v1}
    container_name: gitvane-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000/api/v1}
      - NEXT_TELEMETRY_DISABLED=1
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
  repos_volume:
  huggingface_cache:
